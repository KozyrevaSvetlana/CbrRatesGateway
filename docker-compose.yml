# Локальный запуск: docker compose up --build
# Swagger UI: http://localhost:8080/swagger
services:
  api:
    build: .
    image: cbr-rates-gateway:local
    ports:
      - "8080:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ConnectionStrings__Redis: "redis:6379,abortConnect=false,connectTimeout=2000,syncTimeout=1000"
      Swagger__Enabled: "true"
      # Логи в JSON (удобно для ELK/Loki) вместе со scope: RequestedDate, CurrencyCode, TraceId
      Logging__Console__FormatterName: "json"
      Logging__Console__FormatterOptions__IncludeScopes: "true"
    depends_on:
      - redis

  redis:
    image: redis:7-alpine
    command: ["redis-server", "--maxmemory", "128mb", "--maxmemory-policy", "allkeys-lru"]
    ports:
      - "6379:6379"
